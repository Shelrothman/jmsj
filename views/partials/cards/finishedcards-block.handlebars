<div class="card bg-secondary mt-2">
    <div class="card-body">
        <h5 class="card-title">{{this.title}}</h5>
        <p class="card-text">{{this.platform}}</p>
        <button data-id="{{this.id}}" type="submit" class="btn btn-default bg-light btn-sm bt-sm finishwatch">Finished
            Watching
        </button>
    </div>
</div>

